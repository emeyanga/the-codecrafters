## Go Arrays

I learned that arrays store multiple values of the same type, but their size must be fixed. This means that arrays are fixed but useful in certain situations.

Example:
```go
var numbers = [3]int{1, 2, 3}
```